name: Deploy (Stage)

# Shell workflow — deployment for this project is custom and not the standard
# pipeline. Replace the placeholder step below with the real deploy process.
on:
  push:
    branches:
      - stage

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Placeholder
        run: |
          echo "Deployment for stage is not configured yet."
          echo "This project deploys differently from the standard pipeline."
          echo "Add the real deployment steps here."
