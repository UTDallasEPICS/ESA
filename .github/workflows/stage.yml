name: Deploy (Stage)

# Shell workflow — deployment for this project is custom and not the standard
# pipeline. Fill in the real steps in deploy.yml.
on:
  push:
    branches:
      - stage

jobs:
  deploy:
    uses: ./.github/workflows/deploy.yml
    with:
      environment: stage
    secrets: inherit
