name: Deploy Project Environment

# Shell reusable workflow, called by prod.yml (prod) and stage.yml (stage).
# Deployment for this project is different from the standard build ->
# migrate -> push -> deploy pipeline, so the real steps are intentionally
# left out. Replace the placeholder step below with the project-specific
# deployment process.
on:
  workflow_call:
    inputs:
      environment:
        description: "Target environment (prod or stage)"
        required: true
        type: string

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Placeholder
        run: |
          echo "Deployment for '${{ inputs.environment }}' is not configured yet."
          echo "This project deploys differently from the standard pipeline."
          echo "Add the real deployment steps here."
